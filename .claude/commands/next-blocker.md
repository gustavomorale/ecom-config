---
description: Pick up the next open item from "Blocking a public listing" in CLAUDE.md
argument-hint: "[blocker number, optional]"
---
Read the "Blocking a public listing" section of CLAUDE.md. Work on blocker $ARGUMENTS if
given, otherwise the first one not yet solved in the code.

1. Check the current code and say briefly what's already there and what's missing.
2. Propose a plan in a few bullets and wait for my go-ahead.
3. After I approve: implement, run `npm run check`, keep the single-file MVP in sync
   (see /sync-mvp), and update CLAUDE.md if the blocker is resolved.
Respect the Decisions and Conventions sections.
