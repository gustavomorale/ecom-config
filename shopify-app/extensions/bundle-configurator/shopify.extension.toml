# Theme app extension — app block that mounts the configurator on any page.
# Scaffold: `shopify app generate extension --type theme_app_extension` produces the
# same layout; keep blocks/ and assets/ as the source of truth.
name = "bundle-configurator"
type = "theme"
uid = "f81dd01f-1a3a-a4a7-f4df-7e289a2c5a3de815813a"
